spring:
  config:
    import: submodules/security-common.yml
  profiles:
    group:
      main:
      release:
      local:
      performance:
    default: local
  jpa:
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

  servlet:
    session:
      cookie:
        same-site: none

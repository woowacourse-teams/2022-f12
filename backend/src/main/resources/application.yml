spring:
  config:
    import: submodules/security-common.yml
  profiles:
    group:
      main:
      release:
      local:
    default: local
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

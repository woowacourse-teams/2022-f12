spring:
  config:
    import:
      - classpath:/submodules/application-auth.yml
      - classpath:/submodules/application-h2.yml
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

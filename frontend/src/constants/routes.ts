const ROUTES = {
  HOME: '/',
  PRODUCTS: '/products',
  PRODUCT: '/product',
  REGISTER: '/register',
  LOGIN: '/login',
  PROFILE: '/profile',
  PROFILE_SEARCH: '/profile/search',
  NOT_FOUND: '*',
} as const;

export default ROUTES;
